# 强制 shell 脚本与 CSV 在任何平台 checkout 都是 LF：
# 集群侧 git 若开了 autocrlf，shebang 里的 `bash\r` 会让 ./tools/*.sh 直接
# 报 "/usr/bin/env: 'bash\r': No such file or directory"（diag v1 的 E3 实录）。
*.sh text eol=lf
*.csv text eol=lf
